import 'package:flutter/material.dart';

class AppColors {
  // Primary and secondary colors
  static Color primaryColor = Colors.blue.shade200; // Example purple
  static Color primaryVariant = Colors.blue.shade300;
  static Color secondaryColor = Colors.blue.shade300;

  // Background colors
  static Color background = Colors.blue.shade100; // Light background
  static Color surface = Color(0xFFFFFFFF); // Card or component background

  // Text colors
  static Color primaryText = const Color.fromARGB(255, 3, 42, 101); // Dark text
  static Color secondaryText =
      const Color.fromARGB(255, 32, 24, 42); // Subtle text

  // Icon colors
  static Color iconColor = Color(0xFF6200EE);
}
